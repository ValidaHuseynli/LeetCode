package org.example;

import java.util.*;

public class Tester {
    public boolean isPerfectSquare(int num) {

        double sqrt = Math.sqrt(num);
       if(sqrt*sqrt==(double)num){
           return true;
       }return false;

    }
}
